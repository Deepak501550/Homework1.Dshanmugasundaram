import sympy as sp

# Define the variable
x = sp.symbols('x')

# Define the quadratic function
f = (x + 1)**2 * (x + 3)**2

# Expand the equation
expanded_eq = sp.expand(f)

# Find the roots
roots = sp.solve(expanded_eq, x)

# Print the roots
for root in roots:
    print(f"Root: {root}")
